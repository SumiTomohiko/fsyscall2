
cmd="accept4_SOCK_CLOEXEC/accept4_SOCK_CLOEXEC"
sock="${tmp_dir}/$$.accept4_SOCK_CLOEXEC"
args="${sock}"
expected_status="0"

precmd() {
	rm -f "${sock}"
}

postcmd() {
	rm -f "${sock}"
}

# vim: filetype=sh
