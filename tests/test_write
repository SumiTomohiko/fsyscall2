
msg="foo"
cmd="write/write"
args="1 ${msg}"

test_status()
{
	return 0
}

test_stdout()
{
	local stdout

	stdout="${1}"
	if [ "${stdout}" = "${msg}" ]; then
		return 0
	fi
	return 1
}

# vim: filetype=sh
