
OBJS=	${SRCS:.S=.o}

all: ${LIB}

${LIB}: ${OBJS}
	${AR} cq ${.TARGET} ${OBJS}

${OBJS}: ${ADDITIONAL} Makefile

LST=	${.ALLSRC:M*.S:.S=.lst}

.S.o:
	nasm -f ${FORMAT} -o ${.TARGET} -l ${LST} ${.ALLSRC:M*.S}

clean:
	rm -f ${OBJS} ${LIB} *.lst

# vim: filetype=make
