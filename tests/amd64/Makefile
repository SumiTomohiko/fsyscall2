
LIBDIR=	do_stat_test print_stat_member lib tiny_runtime
SUBDIR=	${LIBDIR} access close close_twice dup dup_and_close exit fcntl fstat \
	fstatfs getdirentries getegid geteuid getgid getpid getuid ioctl \
	issetugid lseek lstat open pread read readlink select select2 socket \
	stat write writev

.include <bsd.subdir.mk>
