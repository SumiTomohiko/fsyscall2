
LIBDIR=	do_stat_test print_stat_member lib tiny_runtime
SUBDIR=	${LIBDIR} access close close_twice exit fstat getuid ioctl issetugid \
	lseek lstat open pread read readlink select select2 stat write writev

.include <bsd.subdir.mk>
